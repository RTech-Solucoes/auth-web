import { describe, it, expect } from "vitest";

// Teste simples para verificar se o Vitest está configurado corretamente
describe("Vitest", () => {
  it("deve estar funcionando", () => {
    expect(1 + 1).toBe(2);
  });
});
